2019-12-23-151504  -- sp_CheckSavePlanGR ex Rollback Value cannot be null.
Parameter name: source
2019-12-23-151750  -- sp_CheckSavePlanGR ex Rollback Object reference not set to an instance of an object.
2019-12-23-152530  -- sp_CheckSavePlanGR ex Rollback Object reference not set to an instance of an object.
2019-12-23-152921  -- sp_CheckSavePlanGR ex Rollback Cannot deserialize the current JSON object (e.g. {"name":"value"}) into type 'System.Collections.Generic.List`1[GRBusiness.PlanGoodsReceive.PlanGoodsIssueItemPopViewModel]' because the type requires a JSON array (e.g. [1,2,3]) to deserialize correctly.
To fix this error either change the JSON to a JSON array (e.g. [1,2,3]) or change the deserialized type so that it is a normal .NET type (e.g. not a primitive type like integer, not a collection type like an array or List<T>) that can be deserialized from a JSON object. JsonObjectAttribute can also be added to the type to force it to deserialize from a JSON object.
Path 'ClassName', line 1, position 13.
2019-12-23-153033  -- sp_CheckSavePlanGR ex Rollback Cannot deserialize the current JSON object (e.g. {"name":"value"}) into type 'System.Collections.Generic.List`1[GRBusiness.PlanGoodsReceive.PlanGoodsIssueItemPopViewModel]' because the type requires a JSON array (e.g. [1,2,3]) to deserialize correctly.
To fix this error either change the JSON to a JSON array (e.g. [1,2,3]) or change the deserialized type so that it is a normal .NET type (e.g. not a primitive type like integer, not a collection type like an array or List<T>) that can be deserialized from a JSON object. JsonObjectAttribute can also be added to the type to force it to deserialize from a JSON object.
Path 'ClassName', line 1, position 13.
2019-12-23-153802  -- sp_CheckSavePlanGR ex Rollback Cannot deserialize the current JSON object (e.g. {"name":"value"}) into type 'System.Collections.Generic.List`1[GRBusiness.PlanGoodsReceive.PlanGoodsIssueItemPopViewModel]' because the type requires a JSON array (e.g. [1,2,3]) to deserialize correctly.
To fix this error either change the JSON to a JSON array (e.g. [1,2,3]) or change the deserialized type so that it is a normal .NET type (e.g. not a primitive type like integer, not a collection type like an array or List<T>) that can be deserialized from a JSON object. JsonObjectAttribute can also be added to the type to force it to deserialize from a JSON object.
Path 'errors', line 1, position 10.
2019-12-23-153959  -- sp_CheckSavePlanGR ex Rollback Cannot deserialize the current JSON object (e.g. {"name":"value"}) into type 'System.Collections.Generic.List`1[GRBusiness.PlanGoodsReceive.PlanGoodsIssueItemPopViewModel]' because the type requires a JSON array (e.g. [1,2,3]) to deserialize correctly.
To fix this error either change the JSON to a JSON array (e.g. [1,2,3]) or change the deserialized type so that it is a normal .NET type (e.g. not a primitive type like integer, not a collection type like an array or List<T>) that can be deserialized from a JSON object. JsonObjectAttribute can also be added to the type to force it to deserialize from a JSON object.
Path 'errors', line 1, position 10.
2019-12-23-154635  -- sp_CheckSavePlanGR ex Rollback Unexpected character encountered while parsing value: <. Path '', line 0, position 0.
2019-12-23-154842  -- sp_CheckSavePlanGR ex Rollback Cannot deserialize the current JSON object (e.g. {"name":"value"}) into type 'System.Collections.Generic.List`1[GRBusiness.PlanGoodsReceive.PlanGoodsIssueItemPopViewModel]' because the type requires a JSON array (e.g. [1,2,3]) to deserialize correctly.
To fix this error either change the JSON to a JSON array (e.g. [1,2,3]) or change the deserialized type so that it is a normal .NET type (e.g. not a primitive type like integer, not a collection type like an array or List<T>) that can be deserialized from a JSON object. JsonObjectAttribute can also be added to the type to force it to deserialize from a JSON object.
Path 'ClassName', line 1, position 13.
